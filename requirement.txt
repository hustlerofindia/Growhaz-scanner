# GROWHAZ Security Testing Tool requirements
# Python 3.8 or higher recommended

requests>=2.31.0
beautifulsoup4>=4.12.0
cryptography>=41.0.0
PyJWT>=2.8.0

# Optional: Playwright for JavaScript crawling
# Install with: pip install playwright && playwright install chromium
playwright>=1.40.0
